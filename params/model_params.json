{
    "data_fn": "lambda x: torch.exp(torch.sin(x[:, [0]]) + x[:, [1]]**3)", 
    "num_samples_train": 800,
    "num_samples_val": 200,
    "num_samples_test": 200,
    "KAN_layers_nodes": [2, 6, 3, 1],
    "num_grids": 12,
    "KAN_activation_layers": "SFS",
    "addbias": true,
    "batch_size": 4,
    "lr": 1e-02,
    "optimizer_weight_decay": 1e-5,
    "scheduler_step_size": 5,
    "scheduler_gamma": 0.5,
    "epochs": 500,
    "patience_for_early_stop": 10
}